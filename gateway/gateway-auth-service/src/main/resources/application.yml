server:
  port: 9001
  servlet:
    context-path: /auth
spring:
  application:
    name: auth-service #应用/微服务名字
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #nacos服务器地址
        username: nacos #用户名密码
        password: nacos
